<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to right, #f44336, #c62828); /* Gradiente de colores rojos */
            color: white;
            text-align: center;
            padding: 50px;
        }

        h1 {
            font-size: 48px;
            margin-bottom: 10px;
        }

        p {
            font-size: 24px;
            background: rgba(255, 255, 255, 0.2);
            display: inline-block;
            padding: 10px;
            border-radius: 15px;
            color: red; /* Color del texto rojo */
        }
    </style>
</head>
<body>
    <h1>Ha ocurrido un error</h1>
    <% if (error) { %>
        <p><%= error %></p>
    <% } %>
</body>
</html>
